# Operators: Comparison

> `<` less than
> `<=` less than or equal to
> `>` greater than
> `>=` greater than or qual to
> `==` equal (loose)
> `===` equal (strict)
> `!=` not equal (loose)
> `!==` not equal (strict)
> `?` ternary
